pipeline {
	triggers {
		cron('*/2 * * * *')
	}
	agent {
		label 'nodejs'
	}
	stages {
		stage('check drift') {
		  steps {
			sh '''
			oc diff -k samples | tee drift.txt
 			cat drift.txt
			! test -s drift.txt
			'''
		  }
		}
	}
}
